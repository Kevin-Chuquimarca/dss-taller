spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dss_taller
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
